# Cucumber runtime properties
# Override tags or plugin list from the command line:
#   mvn test -Dcucumber.filter.tags="@smoke"
#   mvn test -Dcucumber.filter.tags="@api and not @WIP"

cucumber.publish.quiet=true
cucumber.publish.enabled=false
